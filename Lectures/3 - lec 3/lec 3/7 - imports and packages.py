"""
    3:  paradigms: make sense
        syntax: ease of usage / quality of life
        standard library: the strength of the standard library.

        most commonly used packages in the standard library:
            math
            re
            datetime
            os
            random
            sys
            typing


        regex:
            ^: this is the start of the string
            $: this is the end of the string
            +: one or more
            *: zero or more
            ?: zero or one time

        https://regex101.com/
"""
import os
import re
import pathlib
from math import floor, ceil, pi, sqrt
from datetime import datetime, date, timedelta

# os.getenv("KEY")
# os.path
#print(os.environ)

# print(os.path.dirname(os.path.abspath("..")))


# my_str = "Kareem"
#
# print(re.search("ee", my_str))
# print(re.match("Kar", my_str))
# print(re.fullmatch("Kar", my_str))


# import sys
#
# print(sys.getwindowsversion())








