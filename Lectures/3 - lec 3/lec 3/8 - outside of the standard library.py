
"""
    packages:
        use pip, it downloads by default from pypi

        # how to install a package:
            pip install {package_name}
            pip install requests


        # how to know the installed packages
            pip freeze

            requirements.txt

            pip freeze > requirements.txt

            virtual environment:
                goal: to separate the projects required libraries from other projects

                how to use:
                    creation: python -m venv
                    activation:
                            linux: source ./.venv/scripts/activate
                            windows: ./.venv/scripts/activate

        the order of packages exploration:
            can be seen using sys.path
"""











